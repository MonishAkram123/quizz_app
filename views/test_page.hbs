<!-- TODO: Apply CSS to the form and whole document to make it look more attactive Render contents send by server and display here-->
<!DOCTYPE html>
<HEAD>
	<Title>Questions</Title>
	<link href = "css/style.css" rel="stylesheet" type="text/css">
</HEAD>

<body>
	<form action="/logout" method="POST">
		<button type="submit">Logout</button>
	</form>
	<form action="/quiz/submit" method="POST">
	<input type="hidden" name="test_id" value="{{data.test_id}}" />
	<ol>
		{{#each data.questions}}
		<li>{{question}}</li>
		<ol type="a">
			{{#each options}}
				<li>
				<input type="radio" name="{{../id}}" value="{{this}}" required="true">
				<label for="{{this}}">{{this}}</label>
				</li>
			{{/each}}
		</ol>
		{{/each}}
	</ol>
	<button type="submit">Submit test</button>
	</form>
</body>
